# Copy to .env and fill in. .env is git-ignored (never commit real keys).
# LLM provider key — default provider is Anthropic (see config/config.json llm.provider).
ANTHROPIC_API_KEY=

# Optional alternative providers (only if you switch llm.provider):
OPENAI_API_KEY=
GEMINI_API_KEY=
